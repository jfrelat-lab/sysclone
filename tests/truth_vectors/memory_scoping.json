{
  "suite": "CORE: Memory, Types & Structures",
  "vectors": [
    {
      "name": "Default Implicit Typing (SINGLE)",
      "syntax": "Variable = value",
      "description": "By default, QBasic treats all undeclared variables without a type suffix as Single-precision floats (!). They retain fractional values without rounding.",
      "example": {
        "code": [
          "DefaultFloat = 3.14",
          "PRINT DefaultFloat"
        ],
        "output": " 3.14 "
      },
      "quirks_and_tests": {
        "description": "Ensures the engine natively defaults to Single precision when no DEF type or suffix is active.",
        "setup": [
          "DefaultFloat = 3.14"
        ],
        "assertions": [
          { "var": "DefaultFloat", "val": 3.14, "type": "number" }
        ]
      }
    },
    {
      "name": "DEFINT Statement",
      "syntax": "DEFINT letter[-letter]",
      "description": "Sets the default data type for variables to Integer.",
      "example": {
        "code": [
          "DEFINT A-Z",
          "Score = 10.8",
          "PRINT Score"
        ],
        "output": " 11 "
      },
      "quirks_and_tests": {
        "description": "Forces all untyped variables starting with specified letters to automatically become integers. Floating-point assignments to these variables are mathematically rounded (Banker's rounding).",
        "setup": [
          "DEFINT A-Z",
          "ImplicitIntA = 2.5",
          "ImplicitIntB = 2.6"
        ],
        "assertions": [
          { "var": "ImplicitIntA", "val": 2, "type": "number" },
          { "var": "ImplicitIntB", "val": 3, "type": "number" }
        ]
      }
    },
    {
      "name": "DEFSNG Statement",
      "syntax": "DEFSNG letter[-letter]",
      "description": "Sets the default data type for variables to Single-precision float.",
      "example": {
        "code": [
          "DEFSNG A-Z",
          "Score = 10.8",
          "PRINT Score"
        ],
        "output": " 10.8 "
      },
      "quirks_and_tests": {
        "description": "Forces untyped variables to default to Single-precision. This preserves fractional values without mathematical rounding. While QBasic defaults to Single precision natively, DEFSNG is used to explicitly override a previous DEFINT.",
        "setup": [
          "DEFINT A-Z",
          "DEFSNG A-Z",
          "ImplicitSng = 5.8"
        ],
        "assertions": [
          { "var": "ImplicitSng", "val": 5.8, "type": "number" }
        ]
      }
    },
    {
      "name": "Explicit Type Suffix Override",
      "syntax": "Variable[%, &, !, #, $]",
      "description": "Explicit type declaration suffixes.",
      "example": {
        "code": [
          "DEFINT A-Z",
          "PRINT 5.5!"
        ],
        "output": " 5.5 "
      },
      "quirks_and_tests": {
        "description": "Even if DEFINT is active, appending a specific type suffix (like ! for Single Precision Float) overrides the implicit rule and preserves the fractional value.",
        "setup": [
          "DEFINT A-Z",
          "OverrideFloat! = 3.14"
        ],
        "assertions": [
          { "var": "OverrideFloat!", "val": 3.14, "type": "number" }
        ]
      }
    },
    {
      "name": "Memory Aliasing (DIM AS STRING)",
      "syntax": "DIM variable AS type",
      "description": "Declares a variable and allocates memory space.",
      "example": {
        "code": [
          "DIM Name AS STRING",
          "Name = \"NIBBLES\"",
          "PRINT Name$"
        ],
        "output": "NIBBLES"
      },
      "quirks_and_tests": {
        "description": "Declaring a variable with an explicit type (e.g., AS STRING) internally links it to its MS-DOS suffixed counterpart. Writing to the base name updates the suffixed name, as they share the exact same memory pointer.",
        "setup": [
          "DIM AliasTest AS STRING",
          "AliasTest = \"NIBBLES\"",
          "Extracted$ = AliasTest$"
        ],
        "assertions": [
          { "var": "Extracted$", "val": "NIBBLES", "type": "string" }
        ]
      }
    },
    {
      "name": "Fixed-Length String Padding",
      "syntax": "DIM variable AS STRING * length",
      "description": "Declares a string with a fixed memory length.",
      "example": {
        "code": [
          "DIM S AS STRING * 5",
          "S = \"HI\"",
          "PRINT S; \"THERE\""
        ],
        "output": "HI   THERE"
      },
      "quirks_and_tests": {
        "description": "Allocating a Fixed-Length String locks a specific memory block. If a shorter string is assigned, it is automatically padded with spaces on the right to fill the block.",
        "setup": [
          "DIM PadStr AS STRING * 5",
          "PadStr = \"HI\"",
          "PadLen = LEN(PadStr)"
        ],
        "assertions": [
          { "var": "PadStr", "val": "HI   ", "type": "string" },
          { "var": "PadLen", "val": 5, "type": "number" }
        ]
      }
    },
    {
      "name": "Fixed-Length String Truncation",
      "syntax": "DIM variable AS STRING * length",
      "description": "Prevents memory overflow during assignment.",
      "example": {
        "code": [
          "DIM S AS STRING * 3",
          "S = \"123456\"",
          "PRINT S"
        ],
        "output": "123"
      },
      "quirks_and_tests": {
        "description": "If a string longer than the locked memory block is assigned, MS-DOS silently truncates the overflow without throwing an error.",
        "setup": [
          "DIM TruncStr AS STRING * 3",
          "TruncStr = \"123456\""
        ],
        "assertions": [
          { "var": "TruncStr", "val": "123", "type": "string" }
        ]
      }
    },
    {
      "name": "SWAP Statement",
      "syntax": "SWAP variable1, variable2",
      "description": "Exchanges the values of two variables of the same type. Works with scalars, array elements, and UDT properties.",
      "example": {
        "code": [
          "A = 1: B = 2",
          "SWAP A, B",
          "PRINT A; B"
        ],
        "output": " 2  1 "
      },
      "quirks_and_tests": {
        "description": "CRITICAL QUIRKS:\n1. **Memory Type Integrity**: SWAP must work seamlessly across direct environment variables and complex array indices.\n2. **Reference Decoupling**: In memory-managed languages (like JS), swapping object references directly can entangle them. A purist VM must deep-clone the values to safely exchange them.\n3. **Fixed-Length Preservation**: Swapping UDTs or Fixed-Length Strings must mutate the memory IN PLACE, otherwise the fixed-length constraint is permanently destroyed.",
        "setup": [
          "' 1. Scalar Swap",
          "ValA = 10: ValB = 99",
          "SWAP ValA, ValB",
          "",
          "' 2. Array Indices Swap",
          "DIM SwapArr(2)",
          "SwapArr(1) = 42: SwapArr(2) = 77",
          "SWAP SwapArr(1), SwapArr(2)",
          "ArrRes1 = SwapArr(1)",
          "ArrRes2 = SwapArr(2)",
          "",
          "' 3. Fixed-Length String Swap Integrity",
          "TYPE SwapType",
          "  F AS STRING * 4",
          "END TYPE",
          "DIM ST1 AS SwapType, ST2 AS SwapType",
          "ST1.F = \"A\"",
          "ST2.F = \"B\"",
          "SWAP ST1, ST2",
          "ST1.F = \"Z\"",
          "FixedLenAfterSwap = LEN(ST1.F)"
        ],
        "assertions": [
          { "var": "ValA", "val": 99, "type": "number", "message": "First scalar variable must receive the second's value" },
          { "var": "ValB", "val": 10, "type": "number", "message": "Second scalar variable must receive the first's value" },
          { "var": "ArrRes1", "val": 77, "type": "number", "message": "First array index must receive the second index's value" },
          { "var": "ArrRes2", "val": 42, "type": "number", "message": "Second array index must receive the first index's value" },
          { "var": "FixedLenAfterSwap", "val": 4, "type": "number", "message": "Fixed-Length property MUST retain its exact memory padding after a SWAP" }
        ]
      }
    },
    {
      "name": "ERASE Statement",
      "syntax": "ERASE arrayname [, arrayname] ...",
      "description": "Reinitializes elements of static arrays and completely deallocates dynamic arrays from memory.",
      "example": {
        "code": [
          "DIM Arr(5)",
          "Arr(1) = 42",
          "ERASE Arr",
          "PRINT Arr(1)"
        ],
        "output": " 0 "
      },
      "quirks_and_tests": {
        "description": "CRITICAL QUIRKS:\n1. **Static Array Reset**: For statically dimensioned arrays, ERASE does not destroy the array footprint. It iterates and resets numeric arrays to 0, and string arrays to empty strings (\"\").\n2. **Type Protection**: Erasing an array of UDTs containing Fixed-Length strings must softly reset the string's content without destroying its prototype length.",
        "setup": [
          "' 1. Numeric Array Erase",
          "DIM NumArr(2)",
          "NumArr(1) = 42: NumArr(2) = 99",
          "ERASE NumArr",
          "CheckNum1 = NumArr(1)",
          "CheckNum2 = NumArr(2)",
          "",
          "' 2. String Array Erase",
          "DIM StrArr$(1)",
          "StrArr$(1) = \"Hello World\"",
          "ERASE StrArr$",
          "CheckStr$ = StrArr$(1)",
          "",
          "' 3. UDT with Fixed-Length String Erase",
          "TYPE EraseType",
          "  F AS STRING * 4",
          "END TYPE",
          "DIM EArr(1) AS EraseType",
          "EArr(1).F = \"TEST\"",
          "ERASE EArr",
          "EArr(1).F = \"X\"",
          "FixedLenAfterErase = LEN(EArr(1).F)"
        ],
        "assertions": [
          { "var": "CheckNum1", "val": 0, "type": "number", "message": "Numeric array element 1 must be reset to 0" },
          { "var": "CheckNum2", "val": 0, "type": "number", "message": "Numeric array element 2 must be reset to 0" },
          { "var": "CheckStr$", "val": "", "type": "string", "message": "String array elements must be reset to empty strings" },
          { "var": "FixedLenAfterErase", "val": 4, "type": "number", "message": "Fixed-Length String inside UDT MUST survive ERASE and retain its memory size" }
        ]
      }
    }
  ]
}